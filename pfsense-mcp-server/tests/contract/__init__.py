"""Wire-contract test layer.

Validates that the JSON payloads the tools send match the real pfSense REST API
model contract, distilled from the upstream OpenAPI spec (see
``scripts/generate_contract.py`` and ``contract-v2.10.0.json``).
"""
